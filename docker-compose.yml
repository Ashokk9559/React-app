version: '3'

services:
  web:
    image: Ashokk9559/dev-image:latest
    build: .
    ports:
      - "80:80"
    volumes:
      - .:/app
    environment:
      - NODE_ENV=production
    command: ["npm", "start"]
